name: Waka Readme

on:
  schedule:
    # Runs at 12am UTC
    - cron: "0 0 * * *"

jobs:
  update-readme:
    name: Update Readme with Metrics
    runs-on: ubuntu-latest
    steps:
      - uses: athul/waka-readme@master
        with:
          WAKATIME_API_KEY: 29570748-bdb5-4499-bee6-9264d25e04cf
          GH_TOKEN: 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
          REPOSITORY: zidansec/zidansec # optional, By default, it will automatically use the repository who's executing the workflow.
